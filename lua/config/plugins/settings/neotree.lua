return {
  filesystem = {
    filtered_items = {
      visible = true,
      hide_dotfiles = true,
      hide_gitignored = true,
    },
    follow_current_file = { enabled = true },
    never_show = { ".DS_Store" },
  },
  close_if_last_window = true,
  window = {
    width = 35,
  }
}
